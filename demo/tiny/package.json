{
  "private": true,
  "version": "0.0.0",
  "dependencies": {
    "u8-mqtt-packet": "file:../.."
  },
  "devDependencies": {
    "@rollup/plugin-node-resolve": "^7.1.3",
    "qsrv": "^0.1.5",
    "rollup": "^2.7.6",
    "rollup-plugin-terser": "^5.3.0"
  },
  "scripts": {
    "start": "qsrv -p 8081 -- *.html esm/",
    "build": "rollup --config",
    "watch": "npm -s run build -- --watch"
  }
}
